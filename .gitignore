# Anchor / Solana build artifacts
.anchor
target/
test-ledger/

# Rust
**/*.rs.bk

# Editor backups
*~
*.save

# Local environment overrides (keep .env.example, ignore .env)
.env
.env.local
miner-ui/.env
miner-ui/.env.local

# Node
node_modules/
miner-ui/node_modules/
miner-ui/dist/

# Root uses yarn (yarn.lock is committed); miner-ui uses npm so we want to
# commit its package-lock.json for reproducible builds. Anchor only — leading
# slash anchors to the repo root.
/package-lock.json

# Yarn
.yarn/
.pnp.*

# macOS
.DS_Store

# Editor
.idea/
.vscode/
*.swp
*.swo
